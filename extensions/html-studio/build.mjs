import fs from 'node:fs';import path from 'node:path';import {spawnSync} from 'node:child_process';import {createHash} from 'node:crypto';
const files=['store.mjs','server.mjs','public/client.js','public/bridge.js','public/prototype.js'];for(const file of files){const r=spawnSync(process.execPath,['--check',file],{encoding:'utf8'});if(r.status!==0)throw new Error(r.stderr)}for(const file of ['public/index.html','public/style.css','seed/tokens.css','seed/components.html','extension.ts'])if(!fs.statSync(file).isFile())throw new Error('缺失 '+file);fs.mkdirSync('artifacts',{recursive:true});fs.writeFileSync('artifacts/build.json',JSON.stringify({kind:'native-web-workspace',checked:files,files:Object.fromEntries([...files,'public/index.html','public/style.css','extension.ts'].map(f=>[f,createHash('sha256').update(fs.readFileSync(f)).digest('hex')]))},null,2));console.log('真实工作区构建校验通过；无打包依赖。');
