# 选定演示媒体

此页使用用户此前确认的原始录像：62.708333 秒，1920×984，左侧真实 Pi TUI，右侧 HTML。不是后来误选的约 35 秒 UI-only 录像。

原始录像使用实时 PTY 字节显示真正的 Pi TUI，包含真实模型调用 design_workspace inspect/apply、修改落盘、浏览器调参及独立 HTML 导出交互。终端与页面并排属于录制构图，不代表产品内置终端嵌入。聊天面板是本地回显，不是生产模型后端。

公开版只对本机路径和 provider 信息做遮挡。没有重录、重排、加速、新增片头或分段字幕。时长与获批无字幕原版相同。Mac mini 完成全部转码和抽帧。每秒 4 帧、共 251 帧 OCR 扫描未发现所检查的路径和 provider 片段；这不是对每个像素的绝对隐私保证。封面与联系表另行目检。

原始 PTY、会话、OCR 全文及工作区授权 URL 不公开。`tools/demo-video/` 是早期 UI-only 录像的脚本，不是本次选定视频的录制来源。原创页面与媒体遵循仓库 MIT；系统字体未打包。视频无音轨。
